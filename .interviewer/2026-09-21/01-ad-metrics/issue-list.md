# 비공개 문제 목록

정답 공개 전에는 공개·요약·인용하지 않는다.

1. `sort`가 호출자가 소유한 원본 배열을 변경한다.
2. 분모가 0이면 `NaN%`, `Infinity%`, `NaN원` 같은 표시 불가능한 값이 나온다.
3. 숫자 문자열이 유효하지 않을 때 `Number` 결과를 검증하지 않는다.
4. 계산과 표시 형식이 한 함수에 섞여 재사용과 단위 테스트가 어렵다.
